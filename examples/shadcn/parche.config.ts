import { defineConfig } from '@parche/core/config';

export default defineConfig({
  site: 'https://example.com',
  brand: { name: 'Parche shadcn Example', description: 'shadcn/ui components themed by Parche.' },
});
