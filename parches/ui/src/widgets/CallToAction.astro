---
import Heading from 'parche:elements/Heading';
import Card from 'parche:elements/Card';
import Action from '../components/Action.astro';
import type { Props } from './CallToAction.props';

const { title, subtitle, tagline, actions = [] } = Astro.props;
---

<Card shadow="md" class="max-w-3xl mx-auto text-center">
  <Heading {title} {subtitle} {tagline} spacing={false} />
  {actions.length > 0 && (
    <div class="flex flex-col sm:flex-row justify-center gap-4 mt-6">
      {actions.map((action) => (
        <Action action={action} />
      ))}
    </div>
  )}
</Card>
