---
import type { Props } from './Content.props';
import Heading from 'parche:elements/Heading';
import Icon from 'parche:elements/Icon';
import Button from 'parche:elements/Button';
import Image from 'parche:elements/Image';

const {
  tagline,
  title,
  subtitle,
  content,
  image,
  items = [],
  columns = '1',
  isReversed = false,
  isAfterContent = false,
  callToAction,
} = Astro.props;

const colClass =
  columns === '2' ? 'grid-cols-2' : columns === '3' ? 'grid-cols-3' : 'grid-cols-1';
---

<div class={isAfterContent ? 'pt-0 md:pt-0 lg:pt-0' : ''}>
  <Heading {title} {subtitle} {tagline} width="sm" />

  <div class="mx-auto max-w-7xl p-4 md:px-8">
    <div class={`md:flex ${isReversed ? 'md:flex-row-reverse' : ''} md:gap-16`}>
      <!-- Text / items column. Full width when there's no image, so items fill
           the space (a balanced grid) instead of leaving an empty right half. -->
      <div class:list={['self-center', image ? 'md:basis-1/2' : 'w-full']}>
        {content && (
          <div class="mb-12 text-lg text-muted" set:html={content} />
        )}

        {callToAction?.href && (
          <div class="mb-8">
            <Button variant="link" href={callToAction.href} icon={callToAction.icon}>{callToAction.text}</Button>
          </div>
        )}

        {items.length > 0 && (
          <ul class={`grid ${colClass} gap-y-4 md:gap-y-8 gap-x-8`}>
            {items.map((item) => (
              <li class="flex items-start gap-3">
                <span class="flex-shrink-0 flex h-7 w-7 items-center justify-center rounded-full bg-success text-on-success p-1">
                  {item.icon
                    ? <Icon name={item.icon} class="w-4 h-4" />
                    : <Icon name="tabler:check" class="w-4 h-4" />
                  }
                </span>
                <div>
                  {item.title && (
                    <p class="text-lg font-medium leading-6 text-heading ml-2">{item.title}</p>
                  )}
                  {item.description && (
                    <p class="text-muted ml-2 mt-3">{item.description}</p>
                  )}
                </div>
              </li>
            ))}
          </ul>
        )}
      </div>

      <!-- Image column -->
      {image && (
        <div class="mt-10 md:mt-0 md:basis-1/2" aria-hidden="true">
          <div class="relative m-auto max-w-4xl">
            {typeof image === 'string' ? (
              <Fragment set:html={image} />
            ) : (
              <Image src={image.src} alt={image.alt ?? ''} width={640} ratio="1/1" class="mx-auto rounded-lg shadow-lg" />
            )}
          </div>
        </div>
      )}
    </div>
  </div>
</div>
