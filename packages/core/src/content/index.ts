export {
  sectionSchema,
  metadataSchema,
  pageSchema,
  navigationSchema,
  layoutSchema,
  createCollections,
  collections,
} from './schemas.js';

export type { SectionEntry, MetadataEntry, PageEntry, NavigationEntry, LayoutEntry } from './schemas.js';
